<%= form_for(@comentario) do |f| %>
  <% if @comentario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentario.errors.count, "error") %> prohibited this comentario from being saved:</h2>

      <ul>
      <% @comentario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :texto %><br />
    <%= f.text_area :texto %>
  </div>
  <div class="field">
    <%= f.label :calificacion %><br />
    <%= f.number_field :calificacion %>
  </div>
  <div class="field">
    <%= f.label :locals %><br />
    <%=  f.collection_select :locals_id, Local.all, :id, :nombre, :prompt => true %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
